# Entscheidungs-Log (DEC-xx)

> Format: DEC-xx · Frage · Optionen · Entscheidung · Begründung · Rückbauweg
> Keine Entscheidung ohne Eintrag, kein Eintrag ohne Entscheidung. (CLAUDE.md Autonomie-Protokoll §3)

---

### DEC-01 · Wo liegt die Verfassung?
- **Frage:** FINAL_PRD.md lag in `edge-reconciliation/results/`, Framework erwartet Repo-Root.
- **Optionen:** (a) Kopie in Root, Original bleibt; (b) Move; (c) Symlink.
- **Entscheidung:** (a) Kopie in Root.
- **Begründung:** Reconciliation-Artefakte bleiben als abgeschlossener, in sich konsistenter Run unangetastet (Audit-Trail); Root-Kopie ist die Arbeits-Verfassung. Reversibelste Option.
- **Rückbauweg:** Root-Kopie löschen; Original unverändert.

---

### DEC-02 · Output-Pfad der Scinance-2.0-Gate-Läufe (replay_all/evaluate)
- **Kontext:** WP-1 (P1/E-15-Auswertung). `scripts/replay_all.py` schreibt per Default nach `edge_research_framework/results/` (ein Framework-/Doku-Verzeichnis); Survey §4.8 markiert das als DEC-Kandidat. Das Auswertungs-Skript braucht einen stabilen, parametrisierbaren Ergebnis-Eingang.
- **Optionen:** A) Default-Output von `replay_all.py` umbiegen (Bestands-CLI ändern). B) Default unangetastet lassen; das neue `evaluate`-Skript bekommt `--results-path`/`--trades-path` mit Default = bestehender Pfad, plus optionalem `--input-dir` für Scinance-2.0-Läufe nach `handoff_local/results/`.
- **Entscheidung:** B. Bestands-CLI bleibt unverändert; neues Auswertungs-Tooling ist pfad-parametrisiert (Default = `edge_research_framework/results/replay_all_results.json` + `trades_all.csv`), die T2/T3-Runner setzen den Pfad explizit auf `handoff_local/results/`.
- **Begründung:** PRD/Survey schützen Bestands-Verhalten (kein Touch an Schutzgut-naher Replay-CLI); Parametrisierung ist additiv und voll reversibel. Kein Bestands-Konsument bricht.
- **Rückbauweg:** Neues Skript löschen; nichts am Bestand wurde geändert.

---

### DEC-03 · Platzierung des CFAR-Moduls (C-31): Pipeline-Layer vs. research-Paket
- **Kontext:** WP-3 (P4/C-31). Survey §2.P4 nennt zwei Optionen: `layers/l4_pattern/m27_cfar_cyclo.py` (M-xx-Pipeline-Konvention) ODER eigenes Analysepaket `src/bybit_edge/research/c31_cfar/`. C-31 ist laut PRD §3 ein Standalone-Falsifikations-Gate, KEIN Pipeline-Glied (kein `process_ticker`-Bedarf).
- **Optionen:** A) `m27_cfar_cyclo.py` als BaseModule im Pipeline-Baum — folgt M-xx-Konvention, zieht aber Pipeline-/Aggregator-Verdrahtung + Registrierung nach sich (mehr Bestands-Touch, schwerer rückbaubar). B) `src/bybit_edge/research/c31_cfar/` als isoliertes Paket (Cyclic-Spectrum-Schätzer, CFAR-Peak-Detektor, Surrogate-Test) + eigener read-only Replay-Driver über die `trades`-Tabelle.
- **Entscheidung:** B (research-Paket). Neues Verzeichnis `src/bybit_edge/research/` als Heimat für Scinance-2.0-Standalone-Analysemodule (auch WP-4 nutzt es, s. DEC-04).
- **Begründung:** PRD bezeichnet C-31 ausdrücklich als standalone/schnell-DROP-bar; Repo-Konvention (M-xx) ist für Pipeline-Glieder gedacht, nicht für Falsifikations-Gates. research-Paket ist die reversibelste Option (ein Verzeichnis löschen = vollständiger Rückbau, kein Pipeline-/Aggregator-Umbau). Schnittstelle bleibt Welle-2-fähig (CS-07-Footprint kann andocken).
- **Rückbauweg:** Verzeichnis `src/bybit_edge/research/c31_cfar/` + Tests löschen; Pipeline und Aggregator wurden nie berührt.

---

### DEC-04 · C-42-Repro: LightGBM-Dependency vs. sklearn-GBM
- **Kontext:** WP-4 (P2/C-42). `lightgbm` ist weder in `pyproject.toml` noch in der Sandbox vorhanden (Survey §2.P2); die Original-Kestrel-Baseline (research_notes) nutzt `kestrel.training.lightgbm_baseline`. Reproduktionsziel ist die Volatilitäts-Regression (`log(realised_vol_60m)`, Test-R² 0.249), NICHT Direction.
- **Optionen:** A) `lightgbm` als neue optionale Dependency (Repo-Konvention: schwere ML/DL-Deps liegen in `[project.optional-dependencies]`-Extras wie `gpu`/`foundation`/`tuning`) — bleibt nah am Original-Befund, neuer Extra `vol`. B) `sklearn.ensemble.HistGradientBoostingRegressor` (scikit-learn ist auch nicht installiert, aber leichter; KEIN Original-Algorithmus → Reproduktions-Treue fraglich).
- **Entscheidung:** A. Neuer Extra `[project.optional-dependencies].vol = ["lightgbm>=4.0", "scikit-learn>=1.4"]` (sklearn nur für Metrik-/Split-Hilfen, GBM bleibt LightGBM). Modul über einen schmalen `model`-Adapter so kapseln, dass der Estimator austauschbar bleibt.
- **Begründung:** PRD §3 verlangt „C-42-**Reproduktion**" — Reproduktion eines LightGBM-Befundes erfordert LightGBM, sonst misst man ein anderes Modell und das Gate (OOS-R² ≥ 0.15, QLIKE schlägt HAR-RV) wird unvergleichbar. Repo-Konvention für schwere Deps ist der Extra-Mechanismus (reversibel: Extra entfernen). Der Adapter hält B als Notausgang offen, falls LightGBM lokal nicht baubar ist.
- **Rückbauweg:** `vol`-Extra aus `pyproject.toml` streichen, `research/c42_rv/` löschen; Bestands-Deps unverändert. Bei Build-Problem: Adapter auf `HistGradientBoostingRegressor` umstellen (1 Zeile), als DEC-04a protokollieren.

---

### DEC-05 · Heimat des C-42-Repro-Codes
- **Kontext:** WP-4. Survey §2.P2 schlägt `src/bybit_edge/research/c42_rv/` oder `vol/` vor; PRD §7 sagt „C-42 muss in die Pipeline geholt werden" (lebt heute außerhalb des src-Baums).
- **Optionen:** A) `src/bybit_edge/research/c42_rv/` (konsistent mit DEC-03 research-Paket). B) Top-Level `vol/`-Paket.
- **Entscheidung:** A — `src/bybit_edge/research/c42_rv/` (Feature-Engineering 36 Features, purged-WF-Splitter, FDR/BH, QLIKE, LightGBM-Adapter), CLI in `scripts/c42_repro.py`. Datenzugriff read-only via `PersistenceLayer.query_kline`.
- **Begründung:** Ein gemeinsames `research/`-Dach für Scinance-2.0-Standalone-Module (C-31, C-42) hält den Bestands-Pipeline-Baum sauber und ist als Ganzes reversibel. „In die Pipeline holen" (PRD §7) = in den versionierten `src`-Baum + Tests + CLI, nicht zwingend ins Layer-/Aggregator-Wiring.
- **Rückbauweg:** `research/c42_rv/` + `scripts/c42_repro.py` löschen; Bestand unberührt.

---

### DEC-06 · Recording-Engine: eigene Datei/Tabellen statt Collector-Umbau
- **Kontext:** WP-2 (P3/C-36). Survey §2.P3 + §4.6: `insurance.USDT`/`adlAlert` passen nicht ins `{symbol}`-Topic-Template des Bestands-Collectors; Options-Tickers liegen auf einer anderen WS-URL.
- **Optionen:** A) `STREAMS`/`_dispatch` im Bestands-Collector erweitern (Schutzgut-naher Umbau). B) Eigene Recording-Engine `src/bybit_edge/recorder/recording_engine.py` neben dem Collector, eigene Tabellen (`rpi_orderbook`, `insurance_pool`, `adl_alerts`, `premium_index_kline`, `option_tickers`) und eigener Parquet-Unterpfad `data/parquet/recording_f0/` + Ringpuffer-Storage-Deckel.
- **Entscheidung:** B. Collector (`ws_collector.py`) und Bestands-Schema (`db.py`-Tabellen) bleiben unangetastet; die Engine wird additiv aufgebaut und ist streamfähig erweiterbar (Welle-2-Andockpunkt für C-39/C-40/C-33-IV).
- **Begründung:** Schutzgut 1 (laufender Collector) und Schutzgut 3 (Bestands-Parquet) sind nicht verhandelbar; additive Engine + neue Pfade brechen nichts und sind per Datei-/Tabellen-Löschung rückbaubar. Deckt PRD §3-Deckel + Sunset direkt im neuen Modul ab.
- **Rückbauweg:** `recorder/`-Modul, neue Tabellen-DDL und `data/parquet/recording_f0/` entfernen; Collector/Schema nie verändert.

---

### DEC-07 · Storage-Deckel-Default der Recording-Engine (Ratifizierung)
- **Kontext:** WP-2. PRD §3 verlangt eine „fixe GB-Obergrenze, ringpuffer-/rotationsbasiert", nennt aber KEINE Zahl. Der builder hat `DEFAULT_CAP_GB=50.0` als dokumentierte Annahme gesetzt, `--cap-gb`-überschreibbar.
- **Optionen:** A) 50 GB Default (≈ Wochen RPI+IV-Ticks, passt auf übliche Consumer-SSD-Reserven). B) Kleinerer Default (10 GB, konservativer, evtl. zu kurzer Ringpuffer für den Sunset-Review-Horizont 90 Tage). C) Kein Default, Pflicht-Flag (verletzt Ein-Befehl-Runner-Regel der Testpyramide).
- **Entscheidung:** A — 50 GB Default, ratifiziert.
- **Begründung:** PRD-stumm → reversibelste sinnvolle Option; CLI-überschreibbar; Sunset-Review (90 Tage) braucht genug Ringpuffer-Tiefe; C verletzt CLAUDE.md-Runner-Regeln.
- **Rückbauweg:** Konstante ändern oder `--cap-gb` im Runner setzen; kein struktureller Umbau.

---

### DEC-08 · Recorder-Streams: Per-Spec-Subscribe + Phantom-Markierung statt Löschung
- **Kontext:** DIAG2 (2026-06-12). Der T2-Retest des Recorders zeigt im Log eindeutig: `[linear] SUBSCRIBE/REQUEST REJECTED: ret_msg='error:handler not found,topic:adlAlert'`. Bybit lehnt die GESAMTE Subscribe-Request auf der Linear-WS ab, weil ein einziges Topic (`adlAlert`) auf diesem Endpoint nicht existiert (PRD-fable5-Phantom, INC-04/INC-06-Lektion bestätigt). Folge: `rpi_orderbook` und `insurance.USDT` verlieren als Kollateral ihre Subscription, obwohl die Topics vermutlich gültig sind. Ursachen-Code: `recording_engine.run()` bündelte alle Topics einer Transport-Gruppe in EINE Subscribe-Request (all-or-nothing).
- **Optionen:**
  - A) `adl_alerts`-StreamSpec komplett aus `default_stream_specs` entfernen (klein, aber löscht Wissen, kein Audit-Trail; Rückbau bedeutet Re-Implementierung inkl. Normaliser/Schema).
  - B) `phantom: bool = False`-Feld auf `StreamSpec`; phantom-Specs werden beim Run mit WARN geskippt, Schema/Writer/Normaliser bleiben als Wissensspeicher; zusätzlich JEDE StreamSpec als EIGENE Subscribe-Request senden (per-spec subscribe) → ein abgelehntes Topic killt nur seinen eigenen Stream.
  - C) Nur per-spec subscribe, `adl_alerts` unverändert lassen — das Log würde den ERROR weiterhin alle 10 s liefern, ohne Audit-Trail-Entscheidung.
- **Entscheidung:** B. `StreamSpec.phantom: bool = False` eingeführt; `adl_alerts` mit `phantom=True` markiert (Bezug auf Bybit-Antwort 2026-06-12). Die Subscribe-Logik in `_run_ws_transport` sendet jetzt eine Request pro StreamSpec (`_subscribe_per_spec`); phantom-Specs werden mit WARN geskippt. Transports, deren Specs ALLE phantom sind, öffnen gar nicht erst eine Verbindung.
- **Begründung:** Reversibelste Option mit Audit-Trail. Schema (`STREAM_SCHEMAS["adl_alerts"]`), Normaliser (`_norm_adl_alert`), Writer und Tests bleiben unangetastet — falls Bybit das Topic je dokumentiert oder ein anderer Endpoint es liefert, reicht ein `phantom=False` zur Re-Aktivierung. Per-spec subscribe ist die strukturelle Behebung der Kollateral-Klasse von Bugs: kein einzelner abgelehnter Topic-Name kann mehr Sibling-Streams mitreißen. Beide Schritte sind isoliert testbar (siehe `TestPerSpecSubscribeAndPhantom`).
- **Rückbauweg:** `phantom=True` auf `adl_alerts` entfernen → Stream wird wieder subscribed. `_subscribe_per_spec` wieder durch `_subscribe(ws, all_topics)` ersetzen → all-or-nothing-Verhalten zurück. Beides 1-Zeilen-Reverts.
- **Was DEC-08 NICHT entscheidet:** Den Status der Options-WS (`tickers.BTC`/`tickers.ETH`). Die Subscribe-Form ist im Code als korrekt dokumentiert (`repo_survey §2.P3`: Underlying-Topic auf der Option-WS liefert ALLE lebenden Kontrakte für dieses Underlying). Beobachtung: success=true im T2-Log, aber 0 Frames in 5 min. Hypothese: kein Trading-Volume in dem Fenster, oder die Push-Frequenz ist event-getrieben. → braucht eigenen T2-Retest mit verlängertem `RECORDER_SMOKE` (20 min statt 5 min) und ist KEINE DEC-08-Sache.

---

### DEC-09 · C-31-CFAR: Fenster-Tick-Obergrenze (H-03 lief nie durch — 5400s-TIMEOUT 5/5)
- **Kontext:** Overnight 2026-06-14. `C42_WF` lief 5/5 OK (--db-copy-Fix wirkt), aber `C31_CFAR` lief auf ALLEN 5 Symbolen in den 5400s-Timeout (`scinance2-impl/handoff_local/results/upload_20260615/overnight_20260614_150830/C31_CFAR_*.err.log`): DB-Copy + Korrupt-Zeilen-Drop liefen sauber, danach 5400s komplett stumm. Root-Cause im Code verifiziert: `split_windows()` teilt die GESAMTE Tick-Serie eines Symbols in `n_windows` (Default 2). Die Bestands-`trades`-Tabelle des Live-Collectors ist tage-/wochentief (viel älter als die 8h-Aufnahme), also spannt jedes Fenster Tage. `bin_counts()` rastert über die ZEITspanne des Fensters bei `bin_dt_ms` (10/50/100ms) → Millionen bis Milliarden Bins (`MAX_BINS=1e9` fängt nur den Extremfall). Die F-CFAR-Familie = **3 Varianten** (`DEFAULT_BIN_GRID_MS` 10/50/100ms × 1 Threshold 6.0) × `surrogate_test(n_surrogates=200)`, jedes Surrogate baut Timeline + `bin_counts` + SCD neu → 2 Fenster × 3 Varianten × (1+200) ≈ 1206 volle SCD über riesige Arrays. Explosion. Zusätzlich: KEIN Progress-Logging — die err.log war 5400s stumm, ein Hang war nicht diagnostizierbar.
- **Frage:** Wie wird der CFAR-Lauf je Symbol rechenbar gemacht, ohne die vorregistrierten H-03-Torpfosten zu verschieben (Registry-Disziplin §8.2/§8.3: keine Post-hoc-Schwellen-Anpassung)?
- **Optionen:**
  - A) **Deterministische Tick-Obergrenze je Fenster** (`WINDOW_MAX_TICKS=150_000`): die jüngsten `n_windows × max_ticks` Ticks der chronologisch sortierten Serie nehmen, in `n_windows` disjunkte zusammenhängende Fenster teilen. Daten-Scoping, KEINE Schwellen-Änderung.
  - B) **Dauer-Cap je Fenster** (z.B. „letzte N Minuten"): ebenfalls Daten-Scoping, aber bei stark schwankender Tick-Rate ist die Last (= Tick-Anzahl × Bins) NICHT garantiert beschränkt; auf illiquiden Phasen evtl. zu wenig Ticks, auf liquiden zu viele.
  - C) **Nur `n_surrogates` reduzieren:** verändert die Auflösung des Surrogate-Tests (p-Granularität) → schwächt den vorregistrierten Test, faktisch Schwellen-Verschiebung (Registry §8.3-Verstoß). Verworfen.
  - D) **Nur optimieren (vektorisieren/cachen):** verschiebt die Wand, behebt die VERLETZTE Stationaritätsannahme tage-langer Fenster nicht, und bleibt bei Milliarden-Bin-Fenstern letztlich intractable.
- **Entscheidung:** **A — `WINDOW_MAX_TICKS = 150_000`** (neue Konstante in `cyclic_spectrum.py` bei `MAX_BINS`; CLI-Flag `--max-ticks-per-window`, Default = Konstante; durchgereicht `scripts/c31_cfar.py` → `run()` → `split_windows()`). Logik: jüngste `n_windows × max_ticks` Ticks der sortierten Serie, geteilt in `n_windows` disjunkte zusammenhängende Fenster ≤ `max_ticks`; bei weniger Ticks unveränderte gleichmäßige Teilung. Plus Fix 2: Progress-Logging je Fenster/Variante/Surrogate-Schub auf stderr; Pro-Symbol-Timeout in den Runnern von 5400s → 1800s. Die Span-Sanity-Prüfung in `run()` läuft jetzt NACH dem Cap auf den gekappten Fenstern.
- **Begründung:** Reversibelste Option mit korrektem Methoden-Fundament. Zyklostationarität setzt (Quasi-)Stationarität INNERHALB des Fensters voraus; die H-03-Hypothese zielt auf HFT-Skala-Zyklen (Gate: Lead > 50 ms, sub-sekündlich). ~150k Ticks spannen auf liquiden Symbolen zig Minuten — reichlich für sub-sekündliche Zyklik —, während ein tage-langes Fenster die Stationaritätsannahme VERLETZT und obendrein nicht rechenbar ist. Der Bound ist also methodisch KORREKTER, nicht nur schneller. „Jüngste Ticks" ist deterministisch (keine diskretionäre Wahl) und bleibt im Registry-Wortlaut „deterministisch-chronologisch, ≥2 disjunkte Fenster". **Die Gate-Schwellen p≤0.05 / Lead>50ms / Edge>11bps sowie `n_surrogates=200` und BH-FDR α=0.10 bleiben EXAKT wie registriert** — hinzugefügt wird nur ein vorher unspezifizierter Daten-Scoping-Parameter, KEINE Torpfosten-Verschiebung. B verworfen (Last nicht garantiert beschränkt), C verworfen (schwächt den registrierten Test = §8.3-Verstoß), D verworfen (behebt Stationaritätsverletzung nicht).
- **Rückbauweg:** `WINDOW_MAX_TICKS` sehr hoch setzen ODER `--max-ticks-per-window` auf einen riesigen Wert (kein Cap) → ursprüngliches Voll-Serien-Splitten zurück. Konstante + Flag + die Cap-Zeilen in `split_windows()` entfernen = vollständiger Rückbau; `run()`/Surrogate-Statistik unverändert. Progress-Logging ist rein observativ und kann separat entfernt werden.

---

### DEC-10 · C-17/C-41 Lead-Lag-Mess-Gate: Methoden-/Parameter-Festlegung (H-04, KAPITALFREI)
- **Kontext:** Welle-2-WP, neues Paket `src/bybit_edge/research/c17_c41_lead_lag/`. H-04 verlangt ein kapitalfreies Mess-Gate fuer gerichteten Informationsfluss BTC->Alt (Transfer-Entropy ODER Wavelet-Coherence-Phasen-Lead) gegen eine Surrogate-Null, p<=0.05 nach BH-FDR alpha=0.10 ueber F-LEADLAG, Existenz in >=2 disjunkten Fenstern, Lead-Symbol-Stabilitaet. PRD §4 / Registry nennen KEINE konkreten Methoden-Parameter (Resample-Raster, TE-Diskretisierung, Surrogate-Form, Fenster-Tick-Cap) — diese sind vorab-unspezifizierte Implementierungs-Parameter, KEINE Gate-Schwellen.
- **Frage:** Welche (a) Primaer-/Sekundaer-Achse, (b) Surrogate-Null, (c) Return-Diskretisierung, (d) Resample-Raster, (e) Fenster-Tick-Obergrenze waehlen, ohne die vorregistrierten H-04-Torpfosten zu beruehren (Registry-Disziplin §8.3)?
- **Optionen & Entscheidung:**
  - **(a) Achsen:** Primaer = **Shannon-Transfer-Entropy** (C-17), robust ohne scipy-Spezial-Deps, direktional in einer einzigen Statistik; Sekundaer = **numpy-FFT-Morlet-Wavelet-Coherence-Phasen-Lead** (C-41). BEIDE zaehlen in F-LEADLAG. Verworfen: nur eine Achse (Registry verlangt explizit C-17 ∧ C-41 in der Familie).
  - **(b) Surrogate-Null:** **zirkulaerer Block-Shift der Quell-("Leader")-Serie relativ zur Folger-Serie** (Roll um zufaelligen Offset >= 5% der Laenge). Zerstoert die gerichtete Kreuz-Kopplung, erhaelt Marginale UND Autokorrelation beider Serien exakt. Verworfen: Phase-Shuffle (zerstoert auch die Within-Series-Struktur -> testet das Falsche fuer Lead-Lag); reine Permutation (zerstoert Autokorrelation). N=200 Default (analog H-03-Nachtrag).
  - **(c) TE-Diskretisierung:** **Equal-Frequency-Quantil-Binning, n_bins=3 Default** (`DEFAULT_N_BINS`). 3 Bins (down/flat/up) haelt das Joint-Histogramm n_bins^3 klein genug fuer Plug-in-TE aus wenigen tausend Bars bei geringem Bias; equal-frequency ist robust gegen die Heavy-Tails von Krypto-Returns. CLI `--n-bins` ueberschreibbar; jede Bin-Variante zaehlt einzeln in F-LEADLAG.
  - **(d) Resample-Raster:** **1000 ms Default** (`DEFAULT_GRID_MS`), beide Symbole auf ein gemeinsames Bar-Raster ueber ihren ueberlappenden Zeitraum (last-tick/step-sampling, strikt rueckwaerts -> kein Look-ahead). 1s ist fein genug fuer die 30-60s-HFT-Skala der H-04-Hypothese, grob genug fuer stabile TE-Histogramme. CLI `--grid-ms` ueberschreibbar.
  - **(e) Fenster-Tick-Obergrenze:** **WINDOW_MAX_TICKS=150_000 je Symbol je Fenster** (analog DEC-09): die juengsten n_windows×max_ticks Ticks je Symbol, dann gemeinsamer Zeitraum in n_windows disjunkte Gleich-Zeit-Slices. Deterministisch-chronologisch, keine diskretionaere Wahl. Begruendung wie DEC-09: Stationaritaet + Rechenbarkeit, KEINE Schwellen-Verschiebung. CLI `--max-ticks-per-window` ueberschreibbar.
- **Begruendung:** Reversibelste sinnvolle Optionen wo PRD/Registry stumm sind; alle Parameter sind CLI-Flags mit dokumentierten Defaults. Die Gate-Schwellen (p<=0.05, BH-FDR alpha=0.10, >=2 Fenster, Lead-Stabilitaet, n_surrogates=200) bleiben EXAKT wie in H-04 registriert — DEC-10 fixiert nur vorher unspezifizierte Methoden-/Scoping-Parameter. **KAPITALFREIHEIT eingehalten:** kein bps/Edge/PnL/Friction-Code im Modul (Mess-Existenz only; Tradability waere H-04b).
- **Lead-Symbol-Bestimmung:** Das Lead-Symbol je Fenster wird aus der **direktionalen TE-Achse** abgeleitet (Quelle der staerksten FDR-signifikanten TE-Variante), da die WCOH-Coherence-Magnitude symmetrisch und ihr Vorzeichen-Lead rauschanfaellig ist; WCOH-Lead-Vorzeichen nur als Fallback wenn keine TE-Variante signifikant. Smoke-verifiziert (injizierter Lead -> korrektes Lead-Symbol + Lag).
- **Rueckbauweg:** Paket `c17_c41_lead_lag/` + `scripts/c17_c41_lead_lag.py` entfernen = vollstaendiger Rueckbau (standalone, NICHT in Live-Pipeline/replay_backtester verdrahtet, read-only ueber `trades`). Einzelne Defaults via Konstanten/CLI-Flags aenderbar; Bestands-Code unangetastet.

---

### DEC-11 · C-01 OFI-Vorzeichen-Mess-Gate: eigener OFI-Schätzer + Methoden-Festlegung (H-05, INC-02-Anker, KAPITALFREI)
- **Kontext:** Welle-2-WP, neues Paket `src/bybit_edge/research/c01_ofi_sign/`. H-05 verlangt einen kapitalfreien Mess-Test des VORZEICHENS von `sign(OFI_t) -> sign(forward_return_{t+delta})` auf dem Bestands-`trades`-Stream gegen eine Surrogate-Null: WEITER = Vorzeichen `+` (Aggression-Folge, PRD-v1/CS-02) UND p<=0.05 nach BH-FDR alpha=0.10 über F-OFI UND (|corr|>=0.05 ODER Hit-Rate>=0.53) in >=2 disjunkten Fenstern; Vorzeichen<=0 in >=1 Fenster ODER Magnitude verfehlt ODER FDR-p>0.05 = DROP; signifikant INVERSES Vorzeichen = H-05b-Auslöser (kein Bestehen). PRD §4/Registry nennen KEINE konkreten Methoden-Parameter (OFI-Diskretisierung, Grid, Surrogate-Form, Fenster-Tick-Cap) — vorab-unspezifizierte Implementierungs-Parameter, KEINE Gate-Schwellen.
- **Frage:** (a) Eigener OFI-Schätzer oder Bestands-`m2_ofi.py`? (b) OFI-Diskretisierung/Definition, (c) Grid, (d) Surrogate-Null, (e) Fenster-Tick-Cap wählen, ohne die vorregistrierten H-05-Torpfosten zu berühren (Registry-Disziplin §8.3)?
- **Optionen & Entscheidung:**
  - **(a) Schätzer-Quelle: EIGENER Schätzer (`ofi.py`), NICHT `m2_ofi.py`.** Begründung: das Bestands-`M2OFI` (`layers/l1_ingestion/m2_ofi.py`) ist (i) ein Book-Update-OFI (Cont-Kukanov-Stoikov `e_n` aus best-bid/ask Preis+Size-Änderungen), NICHT der trade-flow/Aggressor-OFI den H-05 misst; (ii) trägt die bekannte C-01/C-02-Vertauschung (Survey §2.1) — genau die verdächtige Implementierung, an die der Falsifikationstest NICHT gekoppelt werden darf; (iii) ist stateful/streaming (rollende Deque, Q90-Signal) und konsumiert Orderbook-BBO, nicht die persistierte `trades`-Tabelle. Ein EIGENER, reproduzierbarer, dokumentierter Batch-Schätzer aus der Aggressor-Seite (`trades.side` = Bybit publicTrade-Taker-Seite, gespiegelt von `TradeEvent.signed_volume`) hält den Test unabhängig vom suspekten Modul. **`m2_ofi.py` bleibt read-only, unimportiert, unverändert.** Verworfen: Import von `m2_ofi.py` (koppelt Test an C-01/C-02-Bug + benötigt Orderbook-Stream den das Gate nicht nutzt).
  - **(b) OFI-Definition/Diskretisierung:** trade-flow Aggressor-signiertes Volumen je Zeit-Bin: `signed_qty_i = +vol_i` (side=="Buy", Taker hit Ask) bzw. `-vol_i` (sonst); `OFI_t = sum signed_qty` im Bin `[t, t+grid)`. Leere Bins -> OFI 0 (kein aggressiver Fluss), reguläre Zeitachse. Verworfen: book-update-OFI (s. a); Tick-Rule auf Preisänderungen (redundant, `side` ist die direkte Aggressor-Markierung).
  - **(c) Grid:** **1000 ms Default** (`DEFAULT_GRID_MS`) — fein genug für die 1-5 s-Kurzintervall-Skala des C-01-Claims, grob genug für stabile Pro-Bin-Aggregate. CLI `--grid-ms` überschreibbar.
  - **(d) Surrogate-Null:** **unabhängige Permutation** der OFI-Serie gegen die (fixe) Return-Serie (N>=200 Default, konsistent mit WP-1/Welle-1). Statistik = `|corr|` (Test erkennt Kopplung BEIDER Vorzeichen — die inverse/MM-Replenishment-Richtung muss detektierbar sein, nicht nur die Aggression-Folge). `p = (#{|surr|>=|obs|}+1)/(N+1)`. Verworfen: analytischer Korrelations-p (Permutation gewählt für Konsistenz mit WP-1/Welle-1-Pipelines + Robustheit gegen Krypto-Heavy-Tails).
  - **(e) Fenster-Tick-Cap:** **WINDOW_MAX_TICKS=300_000 je Symbol je Fenster** (analog DEC-09/DEC-10): jüngste `n_windows × max_ticks` Trades, dann gemeinsamer Zeitraum in `n_windows` disjunkte Gleich-Zeit-Slices. Deterministisch-chronologisch, keine diskretionäre Wahl. Begründung wie DEC-09: Stationarität + Rechenbarkeit. CLI `--max-ticks-per-window` überschreibbar.
  - **Kausalität (NO Look-ahead):** `OFI_t` NUR aus Trades in Bin `[t, t+grid)`; `forward_return = log(P(bin_end+delta)/P(bin_end))` mit `P(.)` = letzter Trade-Preis at-or-before (Step/ZOH). Das Forward-Fenster `(bin_end, bin_end+delta]` liegt strikt NACH dem Bin -> teilt keinen Trade mit OFI_t. delta-Universum vorab fixiert {1,5,15,60,300}s (Registry H-05), jede delta-Variante zählt einzeln in F-OFI.
- **Begründung:** Reversibelste sinnvolle Optionen wo PRD/Registry stumm sind; alle Parameter sind CLI-Flags mit dokumentierten Defaults. Die Gate-Schwellen (Vorzeichen `+`, p<=0.05, BH-FDR alpha=0.10 über F-OFI, >=2 Fenster, |corr|>=0.05 ODER Hit-Rate>=0.53) bleiben EXAKT wie in H-05 registriert — DEC-11 fixiert nur vorher unspezifizierte Methoden-/Scoping-Parameter. **KAPITALFREIHEIT eingehalten:** kein bps/Edge/PnL/Sharpe/Friction-Code im Modul (reiner Vorzeichen-/Korrelations-Mess-Test; voller Edge-Test wäre H-05c, braucht L2-Tiefe über Wochen). Der Driver fällt KEIN Gesamturteil; er meldet je Fenster/delta/Symbol jedes Kriterium einzeln plus `sign_direction` (+/-/0) und `inverse_significant` (bool), damit der gate-auditor H-05-Bestehen von einem H-05b-Auslöser (signifikant inverse Richtung) trennen kann (Registry §2: kein Verschieben der Torpfosten).
- **Sandbox-Verify (3 Synthetik-Smokes, seed 42, N=200, 2 Fenster):** (a) Null (beta=0): KEINE FDR-signifikante Variante (kein Falsch-Positiv); (b) Positiv-Aggression (beta>0): Vorzeichen `+`, p=0.005, |corr| 0.35-0.83 (>Floor), Hit-Rate 0.61-0.81 (>0.53), `h05_positive_consistent=true`, `inverse_significant=false`; (c) Inverse/MM-Replenishment (beta<0): Vorzeichen `-`, FDR-signifikant, `inverse_significant=true`, `h05_positive_consistent=false`, Driver meldet NICHT „passed" (= H-05b-Auslöser, NICHT H-05-Bestehen — INC-02-Falle korrekt von echtem Bestehen getrennt).
- **Rückbauweg:** Paket `c01_ofi_sign/` + `scripts/c01_ofi_sign.py` entfernen = vollständiger Rückbau (standalone, NICHT in Live-Pipeline/replay_backtester verdrahtet, read-only über `trades`). Einzelne Defaults via Konstanten/CLI-Flags änderbar; Bestands-Code (insb. `m2_ofi.py`) unangetastet.

---

### DEC-12 · C-07 Permutation-Entropy-Mess-Gate: WINDOW_MAX_BARS + Methoden-/Parameter-Festlegung (H-06, KAPITALFREI)
- **Kontext:** Welle-2-WP (letzter Welle-2-Pilot), neues Paket `src/bybit_edge/research/c07_pe/`. H-06 verlangt ein kapitalfreies, zweistufiges Detektions-Gate auf `kline_1min` (PRD §4 wörtlich „nur Kline, kein Tiefen-Stream"): PRE-Gate ρ >= 0.3 zwischen PE-Drop und nachgelagertem Vol-Cluster im 15-min-Fenster auf >= 2 disjunkten Fenstern (ρ < 0.3 in EINEM Fenster -> DROP); Haupt-Gate bedingte Information PE -> ret/vol_{t+δ} signifikant > Surrogate-Null, p <= 0.05 nach BH-FDR α=0.10 über F-ENTROPY, in >= 2 Fenstern, UND bedingter AUC-Lift >= +0.03 in G1-Fenstern. m = 4 / τ = 1 sind vorab fixiert (claims_register §C-07). Der H-06-Eintrag verlangt, dass der finale `WINDOW_MAX_BARS`-Wert VOR Lauf-Start als datierter Nachtrag UNTER den Eintrag geschrieben wird (analog DEC-09/H-03), NICHT als Schwellen-Verschiebung. PRD/Registry nennen KEINE konkreten Methoden-Parameter (PE-Rolling-Fenster, Vol-Cluster-Definition, MI-Diskretisierung, Surrogate-Form, Bar-Cap) — vorab-unspezifizierte Implementierungs-Parameter, KEINE Gate-Schwellen.
- **Frage:** (0) Welcher `WINDOW_MAX_BARS`-Wert, methodisch begründet (Stationarität, nicht Tractability)? (a) PE-Methode/Rolling-Fenster, (b) Vol-Cluster-Definition fürs PRE-Gate, (c) bedingte-Info-Maß + G1-Definition + Surrogate-Null fürs Haupt-Gate, ohne die vorregistrierten H-06-Torpfosten zu berühren (Registry-Disziplin §8.3)?
- **Optionen & Entscheidung:**
  - **(0) WINDOW_MAX_BARS = 43_200 Bars/Fenster (= 30 Tage 1-min-Klines).** Begründung PRIMÄR STATIONARITÄT, NICHT Tractability: kline_1min hat nur 1440 Bars/Tag/Symbol (≈525k/Jahr) — Rechenbarkeit ist hier (anders als CFAR auf Ticks, DEC-09) KEIN Problem. (1) Genug Bars für robuste PE-Schätzung: bei m=4 gibt es m!=24 Ordinal-Muster; 43_200 Bars liefern bei rollender PE zehntausende Embedding-Vektoren -> stabile PE-Zeitreihe + reichlich (PE-Drop, Vol-Cluster)-Paare für ρ/MI/AUC, dazu >=2 disjunkte Fenster. (2) Kurz genug für Quasi-Stationarität: PE/Bandt-Pompe + der ρ-/MI-/AUC-Vergleich setzen ein hinreichend stationäres Vol-Regime im Fenster voraus; 30 Tage sind ein verteidigbarer Krypto-Vol-Quasi-Stationaritäts-Horizont, ungekappte multi-Monats-Fenster mischen Regimewechsel und sprengen die Stationaritätsannahme. Verworfen: 150_000 (DEC-09-Default-Kandidat, hier ~104 Tage -> Stationarität fraglich); ungekappt (multi-Monats-Regimemischung). CLI `--max-bars-per-window`, Konstante `WINDOW_MAX_BARS` in `driver.py`.
  - **(a) PE-Methode:** Bandt-Pompe Permutation Entropy auf log-Returns, **m=4 / τ=1 FIX** (read-only Konstanten `PE_M`/`PE_TAU` in `perm_entropy.py`, NICHT als CLI-Flag — jede Abweichung wäre NEUE H-06-Zeile, verdict.md §1f „m/τ vorab fixiert sonst DROP"). Gleitende (rolling) PE-Zeitreihe über ein Bar-Fenster (`DEFAULT_PE_WINDOW=240` Bars = 4h, CLI `--pe-window`); normierte Shannon-Entropie der Ordinal-Muster-Verteilung in [0,1]. „PE-Drop" = negative Differenz der rollenden PE (Komplexitäts-Kollaps -> Vorbote Vol-Cluster), gemessen als `-Δ PE` (positive Werte = Drop).
  - **(b) Vol-Cluster-Definition (PRE-Gate):** Realized Vol = Summe der quadrierten 1-min-log-Returns über das **15-min-Forward-Fenster** `(t, t+15min]` (claims_register §C-07 wörtlich „[t, t+15min]"). PRE-Gate ρ = **Spearman-Rank-Korrelation** zwischen PE-Drop_t und forward-RV_{(t,t+15min]} (Rank gewählt: robust gegen die Heavy-Tails von Krypto-RV und gegen die nichtlineare PE-RV-Monotonie; der ρ>=0.3-Floor stammt aus PRD-v1/claims_register und ist als Rang-Assoziations-Floor konservativ und methodenneutral). Strikt kausal: forward-RV nutzt nur Bars NACH t.
  - **(c) Haupt-Gate:** Bedingte Information PE -> ret/vol_{t+δ} = **Mutual Information** I(PE_t ; target_{t+δ}) via Equal-Frequency-Quantil-Binning (`DEFAULT_N_BINS=4`, CLI `--n-bins`); target = forward-RV bzw. |forward-ret| über δ. Surrogate-Null: **zirkulärer Block-Shift der PE-Reihe** relativ zu den Targets (Roll um zufälligen Offset >= 5% der Länge) — zerstört die PE->target-Kopplung, erhält Marginale UND Autokorrelation beider Reihen exakt (verworfen: reine Permutation zerstört Autokorrelation -> zu optimistischer p; Phase-Shuffle zerstört Within-Series-Struktur). N=200 Default (analog H-03/DEC-09). p = (#{MI_surr >= MI_obs}+1)/(N+1). **G1 = oberstes Vol-Quartil der Surrogate-Null-Verteilung, vorab fixiert** (Registry H-06 wörtlich): konkret die Fenster-Bars, deren forward-RV im obersten Quartil liegt; bedingter AUC = AUC eines PE-basierten Vol-Richtungs-Klassifikators (PE-Drop sagt „kommt hohe Vol") auf den G1-Bars, AUC-Lift = AUC(G1) - 0.5 (Baseline Zufall); Lift >= +0.03 registriert. δ-Universum vorab fixiert {1,5,15,60}min (CLI `--lags`); jede (window-length × δ × Symbol)-Variante zählt einzeln in F-ENTROPY.
- **Begründung:** Reversibelste sinnvolle Optionen wo PRD/Registry stumm sind; alle Parameter außer m/τ sind CLI-Flags mit dokumentierten Defaults; m/τ sind read-only-Konstanten (vorab fixiert). Die Gate-Schwellen (PRE-Gate ρ>=0.3 in >=2 Fenstern; Haupt-Gate p<=0.05 nach BH-FDR α=0.10 über F-ENTROPY, >=2 Fenster, AUC-Lift>=+0.03 in G1; n_surrogates=200) bleiben EXAKT wie in H-06 registriert — DEC-12 fixiert nur vorher unspezifizierte Methoden-/Scoping-Parameter (WINDOW_MAX_BARS als datierter Nachtrag unter H-06, NICHT als Torpfosten-Verschiebung, DEC-09-Muster). **KAPITALFREIHEIT eingehalten:** kein bps/Edge/PnL/Sharpe/Friction-Code im Modul (reiner Detektions-/Info-Mess-Test; Tradability wäre H-06b). Der Driver fällt KEIN Gesamturteil; er meldet je Fenster/δ/Symbol jedes Kriterium einzeln (PRE-Gate ρ, surrogate_p, fdr_sig, auc_lift, G1-Fensterzahl), damit der gate-auditor gegen H-06 urteilt.
- **Datenbasis:** read-only `kline_1min` (close), 5 Symbole BTC/ETH/SOL/BNB/XRP; `_open_db_with_timeout` + `--db-copy` + Epoch-ms-Filter auf kline-ts + Progress-Logging, exakt wie DEC-03/DEC-09/DEC-11. `trades` wird NICHT angefasst.
- **Sandbox-Verify (3 Synthetik-Smokes, seed 42, N=200, 2 Fenster):** (a) Null (i.i.d., keine PE-Vol-Kopplung): PRE-Gate ρ < 0.3 UND Haupt-Gate nicht FDR-signifikant (kein Falsch-Positiv); (b) Positiv (GARCH-artig, PE-Drop vor Vol-Burst): PRE-Gate ρ >= 0.3, Haupt-Gate p < 0.05, AUC-Lift >= +0.03; (c) PRE-Gate-Blocker (PE-Signal aber OHNE Vol-Cluster-Kopplung): PRE-Gate ρ < 0.3 (harter PRE-Filter greift vor dem teuren Haupt-Gate). m=4/τ=1 als fix bestätigt.
- **Rückbauweg:** Paket `c07_pe/` + `scripts/c07_pe.py` entfernen = vollständiger Rückbau (standalone, NICHT in Live-Pipeline/replay_backtester verdrahtet, read-only über `kline_1min`). `WINDOW_MAX_BARS`/`--max-bars-per-window` sehr hoch setzen = Cap-Rückbau; einzelne Defaults via Konstanten/CLI-Flags änderbar; Bestands-Code unangetastet.

---

### DEC-13 · C-17/C-41 Lead-Lag-TRADABILITY: Trading-Regel / Friction-Wand / Latenz-Haircut / Horizont (H-04b, **capital_free=FALSE**, erste nicht-kapitalfreie Hypothese)
- **Kontext:** Folge-Pre-Registration H-04b nach GL-006 (H-04 WEITER, kapitalfrei). H-04 bestätigte messbar gerichtete Information BTC→ETH (FDR-sig in beiden Fenstern, Lead-Symbol BTC stabil, signifikante Lags **1–3 s**, Zerfall ab lag5/lag10) — aber das war ein reines Mess-Gate ohne Tradability-Aussage (`capital_free:true`). H-04b ist die im H-04-Gate **ausdrücklich antizipierte** Folge-These (KEIN Data-Snooping): *Ist die gemessene Information nach realistischer Friktion und Latenz überhaupt handelbar?* PRD §4 Z.133 nennt KEINE konkrete Tradability-Schwelle für H-04b — der starke A-priori der Verfassung ist „keine handelbare Kante (abgegraste 30–60s-HFT-Anomalie) → bleibt PARK". Nach CLAUDE.md §3 (DEC-Pflicht bei Unterspezifikation) entscheide ich die reversibelste/konservativste Option und protokolliere hier. **CLAUDE.md §4 / Autonomie-Protokoll §3 Z.30 eingehalten:** H-04b ist ein historischer Backtest mit Kostenmodell auf dem read-only `trades`-Bestand — **KEIN Live-Order-Code, KEIN echtes Geld**; `capital_free=false` markiert NUR, dass das Gate Edge-bps/Friction/Latenz konfrontiert, nicht dass Kapital eingesetzt wird.
- **Frage:** Wo PRD/Registry stumm sind: (a) Trading-Regel, (b) Friction-Wand-Wert + Maker-Behandlung, (c) Latenz-Haircut-Wert + Einfang-Fenster-Definition, (d) Horizont, (e) FDR-Familie — alles ohne Torpfosten-Verschiebung am bestandenen H-04 (Registry §2/§8.3) und so, dass ein WEITER schwer und ehrlich wird (das Gate darf NICHT das H-04-WEITER nachbestätigen).
- **Optionen & Entscheidung:**
  - **(a) Trading-Regel:** Pre-fixed Lead-Lag-Regel auf 1s-Grid (identisch zum H-04-Resampling, DEC-10): BTC-Return über Fenster bis `t` → prädiziert Vorzeichen `ret_ETH` über `[t+lag, t+lag+horizon]`; Position in ETH bei Signal, glattgestellt nach `horizon`. **`lag` NUR aus dem H-04-Survivor-Set 1–3 s** (TE-lag1/2/3 = GL-006-FDR-Survivor; lag5/lag10 zerfallen → NICHT getestet, das wäre Erweiterung ins Nicht-signifikante). Verworfen: freie lag-/Schwellen-Suche (Überanpassung, §8.4).
  - **(b) Friction-Wand:** **11 bps Round-Trip Taker als primäre, verbindliche Wand** (verdict.md §2 Kernrelation wörtlich; im H-03-Gate als „> 11 bps" übernommen). **Maker-Annahme NUR als separater, markierter Sekundär-Fall** (analog iter-4 S2-Maker-Only) — und auch dann ehrlich: Maker-Fills auf 1–3s-Lead-Signal sind **adverse-selection-anfällig** (man wird gefüllt wenn man falsch liegt; verdict.md C-37 „Maker-Quote ≥70 % adversariell selektiert"). Asymmetrie wird dokumentiert; Maker ist NIE Primär-Pass, ein Maker-WEITER bei Taker-DROP wird als „adverse-selection-vorbehaltlich, nicht handelbar bestätigt" markiert. Verworfen: Maker als Primär-Wand (würde die ehrliche Wand umgehen).
  - **(c) Latenz-Haircut (Knackpunkt):** **Vorab fixierter konservativer `latenz`-Default = 300 ms.** Der einzufangende Move ist NUR `ret_ETH` über **`[t+latenz, t+lag+horizon]`**, NICHT der volle Lead-Move über `[t, …]`. Begründung 300 ms: retail-realistische Summe aus Marktdaten-Latenz + Signal-Detektion + Order-Routing zu Bybit (nicht kolokiert); Mitte des konservativen 100–500ms-Bandes; schneidet bei 1s-Lag ~30 % des Lead-Fensters ab — ehrlich, nicht geschönt. Sensitivität {100, 300, 500} ms darf als Robustheits-Spanne MIT-berichtet werden, aber das **Pass-Urteil fällt am Default 300 ms** (kürzere Latenz darf WEITER NICHT erzwingen). Verworfen: 0 ms / kein Haircut (HFT-/kolokiert-Annahme, retail-unrealistisch, würde das H-04-WEITER zirkulär nachbestätigen — genau der zu vermeidende Fehler).
  - **(d) Horizont:** **`horizon` = `lag`** (Default). Der konservativste, freiheitsgrad-ärmste Default — Halte-/Einfang-Zeitraum genauso lang wie der gemessene Lead. Eine Variation von `horizon` würde die F-LEADLAG-TRADE-Familie vergrößern und unter dieselbe BH-FDR fallen. Verworfen: freie Horizont-Suche (verkappte Überanpassung, §8.4).
  - **(e) FDR-Familie:** **F-LEADLAG-TRADE** (NEU, eigenständig, getrennt von F-LEADLAG). H-04b ist im Kern EIN konfirmatorischer Netto-Edge-Vorzeichen-Test; mehrere lag-/horizon-Varianten zählen als eine Familie, BH-FDR α=0.10. Allein laufend nur Familien-intern; F-WAVE2 abgeschlossen, NICHT erweitert (analog H-05b/F-OFI-INV).
  - **Gate-Schwellen (DEC-13 fixiert sie konservativ, da PRD stumm):** **WEITER** = Netto-Edge/Round-Trip (Brutto-Move über `[t+latenz, t+lag+horizon]` − 11 bps − Slippage) **> 0 UND statistisch > 0** (Bootstrap/Surrogate p ≤ 0.05 nach BH-FDR α=0.10) auf **≥ 2 disjunkten Fenstern**. **DROP/PARK** = Netto-Edge ≤ 0 in ≥ 1 Fenster ODER FDR-p > 0.05 → PARK (= PRD-erwarteter Ausgang). Hartes Ein-Fenster-Kriterium (§8.5), kein GRAUBEREICH.
  - **Anti-Gaming-Klausel:** VERBOTEN im Auswertungslauf, um WEITER zu erzwingen: Friction-Wand < 11 bps senken, Latenz < 300 ms verkürzen, oder Lead-Move ohne Latenz-Haircut messen. Jede solche Anpassung = Torpfosten-Verschiebung §2 (der S2/iter-3-Fehler „Signal → handelbar"). WEITER nur gültig bei `latenz ≥ 300 ms` UND `Wand ≥ 11 bps` UND angewandtem Haircut. Andere Annahme → NEUE Hypothese H-04c, NICHT dieses Gate verschieben.
- **Begründung:** Reversibelste, konservativste Optionen wo PRD/Registry stumm sind; jede Wahl ist nach der härteren (PARK-begünstigenden) Seite getroffen, damit ein WEITER schwer und ehrlich ist und das Gate das H-04-WEITER NICHT zirkulär nachbestätigt. `capital_free=false` ist CLAUDE.md-§4-konform, weil es ein historischer Backtest mit Kostenmodell bleibt (kein Live-Order-Code, kein Geldeinsatz) — die Falsifikations-Pipeline des PRD.
- **Rückbauweg:** H-04b-Registry-Eintrag ist append-only (nie editiert); Schwellen/Defaults (Wand 11 bps, latenz 300 ms, horizon=lag) sind in einer künftigen `c17_c41_tradability`-Implementierung CLI-Flags/Konstanten und einzeln änderbar — eine Änderung wäre aber ein NEUER Registry-Eintrag (H-04c), kein Editieren von H-04b. Da JETZT KEIN Code gebaut wird, ist der Rückbau dieser DEC = den H-04b-Eintrag + diesen DEC-13-Block + die CHANGELOG-Zeile entfernen; Bestands-Code (insb. das kapitalfreie `c17_c41_lead_lag/`) bleibt unberührt.


---

### DEC-14 · Cleanup-Schnitt: live_runner DEPRECATE, m8_bocpd-Bug NICHT fixen, bybit_edge.duckdb als Audit-Trail einfrieren
- **Kontext:** Stand 2026-06-23, Welle 1+2 inhaltlich abgeschlossen (alle drei Wellen-1-Hypothesen DROP, Welle 2: H-04 WEITER kapitalfrei, H-04b PARK, H-05 DROP, H-06 DROP). Auf der Nutzer-Maschine trafen zwei Symptome aufeinander: (1) `bybit_edge.live_runner` spammt eine `ValueError: operands could not be broadcast together with shapes (161586,) (161585,)` aus `layers/l3_regime/m8_bocpd.py:147`; (2) ein Neustart von `python -m bybit_edge` scheitert mit `_duckdb.IOException: Zugriff verweigert` auf `data/bybit_edge.duckdb`. Zusätzlich neue Tatsache: der Nutzer betreibt einen separaten **Datenharvester** mit identischem `trades`-Schema, der die Bybit-Streams `publicTrade`, `orderbook` (L2), `tickers`, `rest.fundingRate`, `rest.openInterest`, `allLiquidation` plus Binance + Deribit-Options-IV liefert — und damit den Scinance-1.0-Collector funktional ablöst. Schutzgut #1 (C-36-Recording-Engine für `rpi_orderbook`/`insurance_pool`/`premium_index_kline`) läuft separat weiter und ist von dieser Entscheidung NICHT betroffen. Voll-Inventar in `scinance2-impl/state/CLEANUP_PLAN.md` (19 KEEP / 21 DEPRECATE / 2 REMOVE / 3 REPLACE, 16 sequenzierte TODOs).
- **Frage:** (a) Brauchen wir die Live-Pipeline (`live_runner.py` + `multi_runner.py` + `pipeline.py` + S1-S5-Strategien + L1-L5-Layer-Module) noch? (b) Brauchen wir den Bybit-Collector + `bybit_edge.duckdb` als Schreib-Pfad noch? (c) Müssen wir den `m8_bocpd`-Off-by-One fixen? (d) Wie behandeln wir die existierende `bybit_edge.duckdb` (Audit-Trail aller Welle-1/2-Verdikte) vs. den Übergang zum Harvester?
- **Optionen & Entscheidung:**
  - **(a) `live_runner.py` + zugehöriger Live-Stack → DEPRECATE.** Alle 5 Strategien S1-S5 sind empirisch widerlegt (S1 via C-14-REFUTED, S2 via E-03/E-04/E-16, S3 via GL-004, S4/S5 nie tragfähig; siehe `scinance2-impl/state/WAVE1_FINAL_REPORT.md` §4). Die Live-Schleife ist eine Strategie-Ausführungs-Loop ohne Falsifikations-Programm-Aufgabe. Wir betreiben keinen Live-Handel (CLAUDE.md §4, PRD §6). **Verworfen:** weiterlaufen lassen (kein Mehrwert, blockiert DuckDB-Lock, riskiert Demo-Phantom-Order-Trail in `bybit_edge.duckdb`). **Konsequenz:** `start.bat`-Menü auf 3-Optionen-Version umgebaut (Tests / Recorder-Status / Wave-2-Lauf + REPL + Exit); DEPRECATE-Header-Marker in 21 Legacy-Dateien (`live_runner.py`, `multi_runner.py`, `pipeline.py`, `decision_aggregator.py`, alle 5 `strategies/*.py`, `monitor.py`, `scheduler.py`, `dashboard/{app,data}.py`, `backtester/engine.py`, `training/dataset.py`, `tuning/*.py`, `collector/ws_collector.py`, `persistence/db.py`, `execution/bybit_executor.py`, `risk/budget.py`).
  - **(b) Bybit-Collector + Schreib-Pfad nach `bybit_edge.duckdb` → REPLACE durch Harvester.** Harvester deckt alle vom Collector gesammelten Bybit-Streams ab und liefert zusätzlich Binance + Deribit-Options-IV (Letzteres entsperrt langfristig den C-33-VRP-Vorlauf). Verworfen: Collector + Harvester parallel betreiben (verdoppelt Daten-Sammel-Last, dupliziert Provenance, riskiert Schreib-Lock-Konflikte). **Konsequenz:** Nach TODO-2 (Live-Pipeline gestoppt) gibt es keinen Schreiber mehr für `bybit_edge.duckdb` → der Lock-Konflikt aus Symptom (2) ist strukturell weg, kein Fix nötig.
  - **(c) `m8_bocpd.py`-Off-by-One → NICHT FIXEN.** Wenn (a) durchgezogen wird, fällt der Code-Pfad nicht mehr in Ausführung — der Bug ist Phantom. Verworfen: minimaler atomarer Patch (Runlength-Cap + atomare State-Mutation), da das `m8_bocpd`-Modul ein L3-Regime-Layer-Bestandteil ist und ausschließlich der Live-Pipeline + DROP'd Strategien dient; auch Welle 3 verwendet kein BOCPD (C-08-BOCPD ist laut `scinance2-impl/state/wave3_survey.md` §1 „blockiert / tote Spur"). Falls in fernerer Zukunft eine Welle-N-Hypothese BOCPD aufnimmt, wäre das eine NEUE Hypothese mit eigener Reimplementierung in `src/bybit_edge/research/` (analog Welle-2-Pattern für PE: C-07 in `research/c07_pe/` statt L3-`m7_permutation_entropy.py`). **Aufwand-Vermeidung dokumentiert; reversibel** durch Aufnahme eines minimalen Atomic-Update-Patches in `m8_bocpd.py:compute` falls je nötig.
  - **(d) `bybit_edge.duckdb` → read-only Audit-Bestand einfrieren.** Die Welle-1/2-Gate-Verdikte (insb. GL-001/002/004/005/006/007/008 — die Welle-1/2-Hauptbeweise) basieren auf dem Inhalt dieser Datei zum jeweiligen Lauf-Zeitpunkt. Sie ist Beweismittel, NICHT Werkzeug. **Konsequenz:** Manueller Schritt auf der Nutzer-Maschine (`scinance2-impl/state/CLEANUP_PLAN.md` §6 TODO-6): Kopie nach `data/audit/bybit_edge_frozen_20260623.duckdb` + SHA256 daneben, Original auf read-only oder umbenennen zu `bybit_edge.duckdb.frozen`. Verworfen: löschen (Reproduzierbarkeit der Welle-1/2-Verdikte zerstört); weiter beschreibbar lassen (Driver könnten versehentlich schreiben, Audit-Trail kontaminiert).
- **Begründung:** Reversibelste sinnvolle Option auf jeder Achse (DEPRECATE statt REMOVE belässt Code als Audit-Trail; Audit-Snapshot statt Löschen belässt Datenbestand; nicht fixen-statt-fixen erspart 2-4h Code-Wartung ohne Bedeutung für das Falsifikationsprogramm). Die Entscheidung schließt einen klaren Schnitt zwischen Scinance-1.0-Legacy (DEPRECATE/REPLACE/REMOVE) und Scinance-2.0-Falsifikationsprogramm (KEEP) und macht den Harvester zur einzigen aktiven Bybit-Daten-Quelle (außer C-36-Schutzgut). **CLAUDE.md §4-konform:** kein Live-Order-Code mehr im operativen Pfad, kein Geldeinsatz (DEMO-Mode war auch nur DEMO, aber Pfad wird strukturell entriegelt). PRD §6 (Falsifikations-Pipeline statt Live-Trading) durchgängig respektiert.
- **Akteur-Verteilung:**
  - **Nutzer auf Windows-Maschine (erledigt):** TODO-1 (Zombie-Prozesse killen), TODO-2 (Live-Pipeline stoppen + nicht mehr starten). Noch offen: TODO-4 (`check_recording.py` als Schutzgut-Lebensbestätigung), TODO-6 (`bybit_edge.duckdb` als Audit-Kopie einfrieren).
  - **Orchestrator (heute):** TODO-3 (diese DEC-14), TODO-5 (CLEANUP_PLAN.md committen — erledigt), TODO-7 (`start.bat`-Rebuild), TODO-12 (DEPRECATE-Header-Marker in 21 Dateien). Spätere TODOs (8/9/10/11/13/14/15/16) bleiben sequenziell offen, siehe CLEANUP_PLAN §6.
- **Rückbauweg:** Pro Sub-Entscheidung einzeln rückbaubar.
  - (a) DEPRECATE-Marker entfernen + `start.bat`-Alt-Version aus git-history zurückholen (`git revert <cleanup-commit>`). Live-Pipeline wieder startbar.
  - (b) Collector wieder anschalten = `EXECUTION_ENABLED=true` in `.env`, `python -m bybit_edge` starten. Erfordert dass DuckDB-Lock frei ist (siehe (d)).
  - (c) Bug-Fix wäre minimaler atomarer Patch im `m8_bocpd.py:compute` zwischen Zeile 183 und 217 (alle 5 Sufficient-Stats-Arrays gemeinsam in einem try/except neu allokieren und atomar zuweisen) + Runlength-Cap (`MAX_RL=10_000`). 2-4h Aufwand, Test-Suite (`tests/unit/test_m8_bocpd.py`, 10 Tests) muss grün bleiben.
  - (d) `data/audit/bybit_edge_frozen_20260623.duckdb` zurückkopieren = SHA-Verifikation, `bybit_edge.duckdb.frozen` zurückbenennen. Nur als Notfall-Pfad sinnvoll.


---

### DEC-15 · H-05b-OOS-Fensterwahl: Pre-Discovery-Backfill (April/Mai) statt Post-Discovery
- **Kontext:** H-05b (inverse OFI-Lesart, registriert 2026-06-17) verlangt ≥2 disjunkte OOS-Fenster mit Ausschluss der Entdeckungszelle (ETHUSDT Juni w0 δ1s). Registry-Regel 3 formulierte (als nur Forward-Recording verfügbar war): „bevorzugt frische/erweiterte Daten über den Entdeckungslauf hinaus". Inzwischen liefert der Harvester einen **Backfill bis 2026-03-20** (read-only Junction `data/harvest/`), also auch reichlich PRE-Discovery-Daten. Frage: konfirmatorische Fenster aus Post-Discovery (Juni 18+) ODER Pre-Discovery (April/Mai)?
- **Optionen & Entscheidung:**
  - **(A, GEWÄHLT) Pre-Discovery-Backfill, Fenster A=2026-04-15, B=2026-05-15, je erste 300k Ticks/Symbol.** Sauberes Single-Form-Backfill (keine Backfill/Live-Doppel-Trade-Caveat aus EXPORT.md), temporal unabhängig vom Juni-Entdeckungsregime, alle 5 Symbole DONE, ~30 Tage disjunkt, deterministisch (Monatsmitte).
  - (B, verworfen) Post-Discovery (Juni 18+): liegt nahe am Entdeckungsfenster (06-11..17) → selbes Marktregime, geringere Unabhängigkeit; liegt an/nach der Backfill/Live-Grenze (~06-15) → Doppel-Trade-Risiko; Live-Lücke 06-26. Literarisch näher an „hinaus", methodisch aber schwächer.
  - (C, verworfen) Beide kombiniert (4 Fenster): unnötige Vergrößerung der F-OFI-INV-Familie ohne methodischen Mehrwert; 2 disjunkte saubere Fenster erfüllen das Gate.
- **Begründung:** „Out-of-Sample" = Daten, die die Hypothese NICHT geformt haben. Der H-05-Entdeckungslauf nutzte `bybit_edge.duckdb` (Collector, ~06-11..17, „most recent 300k×2 Ticks") und hat April/Mai-Daten NIE gesehen — weder über den Harvester (separates Archiv) noch über den „most recent"-Split. Pre-Discovery-Backfill ist damit ein vollwertiger, sogar sauberer Holdout (unabhängigeres Regime, einfacheres Datenformat). Regel 3 wird im Geist erfüllt (Daten außerhalb des Entdeckungslaufs), nicht nur im Buchstaben. Die Entdeckungszelle ist per Konstruktion ausgeschlossen (anderer Zeitraum). Reversibelste Option, die das Gate am ehrlichsten testet.
- **Datenbasis:** read-only Harvester-Junction `data/harvest/raw/bybit/publicTrade/symbol=<SYM>/date=<d>/`; Export via `scripts/export_symbol.py` (Harvester-Repo) → flaches `ts_exchange_ms|side|price|size|symbol`-Parquet; gelesen über `c01_ofi_sign.load_trades_file` (ts_exchange_ms-Alias seit cfe6043). KEIN Schreibzugriff auf den Harvester-Baum (Schutzgut-Prinzip).
- **Rückbauweg:** WP-0-Nachtrag unter H-05b + dieser DEC-15-Block entfernen = Fensterwahl wieder offen; die H-05b-Gate-Schwellen sind davon unberührt (nur Datenbasis fixiert, keine Schwelle verschoben). Andere Fensterwahl wäre ein neuer datierter WP-0-Nachtrag VOR dem Lauf, kein Editieren dieses.


---

### DEC-16 · H-05c OFI-Fade-Tradability: Trading-Regel / Friction-Wand / Latenz-Haircut / Horizont / Pass-Zellen (capital_free=FALSE)
- **Kontext:** Folge-Pre-Registration H-05c nach GL-010 (H-05b WEITER, kapitalfrei — inverse OFI-Mess-Existenz auf SOLUSDT δ1s/δ5s, OOS ≥2-Fenster-konsistent). GL-010 benannte die handelbare inverse Kante ausdrücklich als NEUE H-05c (analog der H-04→H-04b-Trennung). PRD/Registry nennen KEINE konkrete Tradability-Schwelle für H-05c; nach CLAUDE.md §3 entscheide ich die reversibelste/konservativste Option und protokolliere hier. CLAUDE.md §4 eingehalten: historischer Backtest mit Kostenmodell auf read-only Harvester-Backfill — KEIN Live-Order-Code, KEIN Geld; capital_free=false markiert NUR Edge/Friction/Latenz-Konfrontation.
- **Frage:** Wo PRD/Registry stumm: (a) Trading-Regel, (b) Friction-Wand + Maker, (c) Latenz-Haircut + Einfang-Fenster, (d) Horizont, (e) Pass-Zellen-Scope, (f) FDR-Familie — ohne Torpfosten-Verschiebung am bestandenen H-05b und so, dass ein WEITER schwer/ehrlich wird.
- **Optionen & Entscheidung:**
  - **(a) Trading-Regel:** Single-Symbol-FADE auf 1s-Grid (OFI-Resampling wie H-05b/DEC-11): Vorzeichen OFI_t prädiziert INVERSES SOL-Return-Vorzeichen über [t+δ_start, t+δ_end]; Position ENTGEGEN OFI (Fade), glatt nach horizon. Verworfen: Follow-Regel (das ist H-05, schon DROP); freie Schwellen-Suche (Überanpassung §8.4).
  - **(b) Friction-Wand:** 11 bps Taker primär (verdict.md §2), Maker nur adverse-selection-vorbehaltlicher Sekundär-Fall, NIE Primär-Pass. Slippage 4 bps (≈15 all-in). Identisch H-04b/DEC-13.
  - **(c) Latenz-Haircut:** latenz-Default 300 ms; Einfang nur [t+latenz, t+δ_end]. Bei δ1s besonders streng (~30% des Fensters weg). Sensitivität {100,300,500}ms MIT-berichtet, Pass am Default 300ms. Identisch H-04b/DEC-13. Verworfen: 0ms/kein Haircut (zirkuläre Nachbestätigung des H-05b-WEITER).
  - **(d) Horizont:** horizon=δ (konservativster, freiheitsgrad-ärmster Default). Verworfen: freie Horizont-Suche (§8.4).
  - **(e) Pass-Zellen-Scope:** NUR SOL-δ1s und SOL-δ5s (die GL-010-≥2-Fenster-inverse-konsistenten Survivor). BTC/ETH/SOL-δ15s nur markierte Robustheit, NICHT urteilstragend (waren in H-05b NICHT ≥2-Fenster-konsistent). Verworfen: Scope-Ausweitung auf nicht-bestätigte Zellen (= Anti-Gaming-Verstoß, verkappte Mehrfachtestung).
  - **(f) FDR-Familie:** F-OFI-INV-TRADE (NEU, getrennt von F-OFI-INV). ≤2 Pass-Zellen × Fenster als eine Familie, BH-FDR α=0.10. F-WAVE2 nicht erweitert.
  - **Gate-Schwellen (DEC-16 fixiert konservativ):** WEITER = Netto-Edge (Brutto inverse-Move über [t+latenz, t+δ_end] − 11 bps − Slippage) > 0 UND statistisch > 0 (Bootstrap p≤0.05 nach BH-FDR über F-OFI-INV-TRADE) in ≥2 disjunkten Fenstern für ≥1 der Zellen SOL-δ1s/δ5s. DROP/PARK = Netto-Edge ≤0 in ≥1 Fenster ODER FDR-p>0.05. Hartes Ein-Fenster-Kriterium, kein GRAUBEREICH.
  - **Anti-Gaming-Klausel:** VERBOTEN zum Erzwingen von WEITER: Wand<11bps / Latenz<300ms / Move-ohne-Haircut / Pass-Zellen-Ausweitung. WEITER nur gültig bei latenz≥300 UND Wand≥11 UND Haircut UND Taker UND Pass-Zelle∈{SOL-δ1s,SOL-δ5s}. Andere Annahme = NEUE H-05d. gate_valid_assumptions-Flag im Output.
- **Begründung:** Reversibelste, konservativste Optionen wo PRD/Registry stumm; jede Wahl nach der härteren (PARK-begünstigenden) Seite, damit WEITER schwer/ehrlich ist und das H-05b-WEITER NICHT zirkulär nachbestätigt wird. Der A-priori ist stark PARK (Mess-Signal schwach: |corr| ~0.01–0.05, prädizierbare Sub-bps-Komponente vs. 11-bps-Wand). Exakt die H-04→H-04b-Lehre, hier noch deutlicher. capital_free=false ist CLAUDE.md-§4-konform (historischer Backtest mit Kostenmodell, kein Live-Order/Geld).
- **Datenbasis:** read-only Harvester-Backfill SOLUSDT (Junction data/harvest), bevorzugt die DEC-15-OOS-Fenster (A@2026-04-15, B@2026-05-15); NICHT data-snooped (Zellen in GL-010 vorab benannt). WINDOW_MAX_TICKS=300000.
- **Rückbauweg:** H-05c-Registry-Eintrag append-only (nie editiert); Schwellen/Defaults (Wand 11, latenz 300, horizon=δ) sind in einer künftigen `c01_ofi_tradability`-Implementierung CLI-Flags/Konstanten — eine Änderung wäre NEUER Eintrag (H-05d), kein Editieren von H-05c. Da JETZT KEIN Code gebaut: Rückbau = H-05c-Eintrag + DEC-16-Block + CHANGELOG-Zeile entfernen; Bestands-Code (insb. kapitalfreies c01_ofi_sign/) unberührt.


---

### DEC-17 · C-06 Cross-Sectional Ergodic Mean-Reversion Mess-Gate: Methoden-/Parameter-Festlegung (H-07, KAPITALFREI)
- **Kontext:** WP-0-Folge Welle 3, Pilot C-06. PRD §4 Z.129 verlangt eine NICHT-triviale MR (simple Sign-Flip via E-04 widerlegt, PRD §6 verboten). Entwurf durch Research-Analyst (`state/c06_h07_draft.md`), Orchestrator-Prüfung bestanden (Nicht-Trivialität via Baseline-Verstärkungs-Anker strukturell gesichert, genau 2 vorab fixierte Achsen, keine Suche, kapitalfrei). PRD/Register sind zu Bar-Länge, L, Crash-Regime-Operationalisierung, Horizont-Menge und Verstärkungs-Kriterium stumm → nach CLAUDE.md §3 reversibelste/konservativste Option, hier protokolliert.
- **Frage:** Wie wird die C-06-„NICHT-triviale MR" ehrlich (≠ E-04-Sign-Flip, forking-paths-frei) als kapitalfreies Mess-Gate operationalisiert?
- **Optionen & Entscheidung:** (1) unkonditionierte MR messen — VERWORFEN (E-04-/§6-verboten, trivial). (2) Regime-Konditionierung mit Achsen-/Schwellen-Suche — VERWORFEN (Variante-A-Forking-Paths, Survey §2.3). (3, GEWÄHLT) 2 vorab fixierte Achsen + Baseline-Verstärkungs-Anker (nicht-überlappende Bootstrap-CIs) + kapitalfrei + ehrliches DROP-A-priori.
- **Vorab fixierte Parameter (KEINE Suche):** Panel BTC/ETH/SOL/BNB/XRP; Bar-Länge 5 min (Last-Price/Bar aus publicTrade); L=12 Bars (M13 „1h"); Z_THRESH=2.5 (§7.5 wörtlich); Achse A |z|≥2.5, Achse B Panel-15min-RV NICHT oberstes Dezil (HMM-Crash-Proxy); Renyi-TE WEGGELASSEN (Freiheitsgrad-Minimierung); Horizonte h∈{1,3,6} Bars; N-Floor ≥30 Ereignisse/Fenster; Surrogate n=200; Fenster A@2026-04-15 + B@2026-05-15 (DEC-15, Kalender-synchronisiert, 2 Tage/Fenster, forward-fill ≤1 Bar); FDR-Familie F-XMR BH-FDR α=0.10.
- **Gate-Schwellen UNVERÄNDERT wie in H-07 registriert:** WEITER = kond. μ_rev>0 UND p≤0.05 FDR über F-XMR UND ≥2-Fenster-Konsistenz UND nicht-überlappende 95%-Bootstrap-CIs (kond > baseline) für ≥1 h in ≥2 Fenstern; sonst DROP; hartes Ein-Fenster-Kriterium, kein GRAUBEREICH. DEC-17 fixiert NUR vorher unspezifizierte Methoden-/Scoping-Parameter, KEINE Torpfosten-Verschiebung (DEC-09/DEC-12-Muster, §8.3).
- **Begründung:** reversibelste Optionen wo PRD stumm (konservativste Bar-Länge/L/Dezil/Horizonte; verteilungsfreies CI-Kriterium ohne zusätzlichen Schwellen-Freiheitsgrad); Renyi-TE weggelassen minimiert Freiheitsgrade; KAPITALFREIHEIT strikt (kein bps/Edge/PnL/Friction). Der Baseline-Verstärkungs-Anker schließt die E-04-Trivial-Lesart strukturell aus dem Erfolgspfad aus — das ist die Nicht-Trivialität. Tradability wäre H-07b (nicht impliziert). A-priori DROP (Survivorship + §6-Historie + 5-statt-20-Symbol-Power-Risiko).
- **Datenbasis:** read-only Harvester-Backfill (Junction data/harvest), 5-Symbol-Panel, DEC-15-Kalenderfenster. KEIN Schreibzugriff auf den Harvester (Schutzgut).
- **Rückbauweg:** H-07-Registry-Eintrag + DEC-17-Block + CHANGELOG-Zeile + `state/c06_h07_draft.md` entfernen = Festlegung wieder offen; da JETZT KEIN Code gebaut wird, kein Code-Rückbau; Bestands-Code (c01_ofi_sign, c07_pe) unberührt.


---

### DEC-18 · H-08 Rang-Über-Dehnung: Definition + Build-Platzierung (KAPITALFREI)
- **Kontext:** GL-012 (H-07 struktureller Power-DROP: |z|>=2.5 auf N=5 unerreichbar, max|z|=2.0). GL-012 antizipiert ausdrücklich eine panel-robuste Über-Dehnungs-Definition als NEUE H-08. Frage: (a) Wie wird die Über-Dehnung panel-robust definiert, ohne eine verkappte Z_THRESH-Absenkung zu sein? (b) Wo lebt der Build (neues Modul vs. Parameter am bestehenden c06_xmr)?
- **Optionen & Entscheidung:**
  - **(a, GEWÄHLT) Rang-basiert und SCHWELLEN-FREI:** Event = das je Bar cross-sectional extremste Symbol (argmax |z|, Gleichstand deterministisch alphabetisch). Verworfen: jede abgesenkte |z|-Schwelle (1.5/1.8/1.9 — alle wären verkappte Z_THRESH-Absenkungen auf DERSELBEN Definition = Registry-§2-Verstoß, und jede konkrete Zahl wäre ein gesuchter Freiheitsgrad). Verworfen: Perzentil-Schwelle auf |z| (z.B. Top-10% der Bars — führt einen neuen Schwellen-Freiheitsgrad ein). Die Rang-1-Definition ist der EINZIGE schwellen-freie Kandidat: null zusätzliche Parameter, auf jedem N erfüllbar, deterministisch. Preis (ehrlich benannt): Verdünnung — Rang-1 feuert jede Bar; die Beweislast liegt vollständig auf dem unveränderten Nicht-Trivialitäts-Anker (CI-Nicht-Überlappung kond vs. baseline).
  - **(b, GEWÄHLT) Erweiterungs-Parameter am bestehenden `c06_xmr`-Modul** (`overextension`-Modus, Werte "z" [H-07-Default, UNVERÄNDERT] | "rank" [H-08]). Verworfen: Modul-Duplikation `c06_xmr_rank/` (im H-04b/H-05c-Fall trennte das Modul ein KAPITALFREIES von einem NICHT-kapitalfreien Gate — hier sind BEIDE Hypothesen kapitalfrei und teilen 95% der Mess-Maschinerie; H-07 lief nie auf Daten [struktureller DROP], es gibt keinen datentragenden Zustand zu schützen). Reversibelste Option: Parameter entfernen = Rückbau; der H-07-Default-Pfad bleibt bit-identisch.
- **Begründung:** (a) folgt zwingend aus dem Torpfosten-Verbot: die Definition ändern ist legitim (neue Hypothese, neue Familie F-XMR-RANK), die Schwelle senken nicht. (b) folgt aus CLAUDE.md §3 (reversibelste Option) + der Kapitalfreiheits-Symmetrie beider Hypothesen. Alle H-07/DEC-17-Parameter (Panel, Bars, L, Achse B, Horizonte, N-Floor, Fenster, Surrogate, CIs) bleiben wörtlich unverändert — DEC-18 fixiert NUR die neue Achse-A-Definition + die Build-Platzierung.
- **Rückbauweg:** H-08-Registry-Eintrag + DEC-18 + CHANGELOG-Zeile entfernen = Registrierung zurück; `overextension`-Parameter entfernen = Code-Rückbau (H-07-Default-Pfad unberührt); F-XMR-RANK-Familie verfällt mit dem Eintrag.


---

### DEC-19 · Bruecke edge-research-v3 -> Scinance-2.0-Hauptregistry (H-09..H-13, Welle 4)
- **Kontext:** Der Cross-Domain-Research-Run (`edge-research-v3/`, eigenes CLAUDE.md, sechs Fachgebiets-Agenten + Unterstuetzungs-Agenten) hat autonom 5 vorregistrierte Hypothesen H-09..H-13 gehaertet und in `edge-research-v3/results/CROSSDOMAIN_PRD.md` + `results/deep_validation/hardened_hypotheses.md` abgelegt (REVIEW-Phase: FREIGEGEBEN, 5/5 PASS 7/7, `results/review.md`). Diese Dateien liegen ausserhalb der Haupt-Registry-Disziplin (`scinance2-impl/state/hypothesis_registry.md`). Frage: wie werden sie formal Teil des Scinance-2.0-Programms, ohne die Torpfosten-Disziplin zu verletzen?
- **Optionen & Entscheidung:**
  - **(a, GEWAEHLT) Woertliche Uebernahme in hypothesis_registry.md als neuer Abschnitt "Welle 4 — Cross-Domain-Track".** Jede Schwelle, jedes Fenster, jede FDR-Familie, jedes Abbruchkriterium 1:1 aus `hardened_hypotheses.md` uebertragen (nicht aus der komprimierteren `CROSSDOMAIN_PRD.md`, da diese selbst nur eine Formatierung von `hardened_hypotheses.md` ist — Primaerquelle bevorzugt). Keine Neuinterpretation, keine Vereinfachung von Zahlen.
  - (b, verworfen) `edge-research-v3/results/` als alleinige Quelle belassen, keine Registry-Uebernahme: wuerde die Welle-4-Implementierung von der etablierten GL-Verdikt-/Audit-Trail-Kette (`gate_log.md`, `hypothesis_registry.md`) abkoppeln — inkonsistent mit der Programm-Disziplin aller 13 Vorgaenger-Verdikte.
  - (c, verworfen) Neue, separate Registry-Datei nur fuer Welle 4: unnoetige Fragmentierung, erschwert den GL-014ff.-Verdikt-Bezug spaeter.
- **Begruendung:** (a) ist reversibel (der Abschnitt kann bei Bedarf identifiziert und entfernt werden, ohne H-01..H-08 zu beruehren) und haelt die Torpfosten-Disziplin durch woertliche Transkription. `edge-research-v3/results/` bleibt als vollstaendiger Herkunfts-Audit-Trail (20 rohe IC-Vorschlaege, 2 Pre-Screens, Scoring, Deconflict-Begruendung, Haertung, Review) unangetastet und wird per Verweis zitiert, nicht dupliziert.
- **GL-Zaehlung:** bleibt bei GL-013. Diese Bruecke ist reine Registrierung, kein Verdikt. GL-014ff. sind fuer den ersten echten Welle-4-Lauf reserviert (`CROSSDOMAIN_PRD.md` §5 Schluss-Klausel).
- **Datenbindung:** alle 5 Hypothesen binden an den externen Harvester (Junction `data/harvest`), 3 sofort testbar (H-09/H-10/H-12), 2 data-gated mit expliziter Entsperr-Bedingung (H-11: >=730 Tage lueckenlose Manifest-Coverage; H-13: 2 vol-regime-disjunkte Live-IV-Snapshot-Tage).
- **Rueckbauweg:** Abschnitt "Welle 4 — Cross-Domain-Track" aus hypothesis_registry.md + dieser DEC-19-Block + wave4_state.md entfernen = vollstaendiger Rueckbau; `edge-research-v3/` bleibt als eigenstaendiger Recherche-Trail unberuehrt (kein Code wurde dort je gebaut).


---

### DEC-20 · Fable-5-Erschoepfung waehrend Welle-4-Build: Fallback auf Sonnet (Session-Modell)
- **Kontext:** Waehrend Phase B (5 parallele Fable-5-Builder fuer H-09..H-13) meldeten die Fortsetzungs-Agenten fuer H-12 und H-13 einen API-Fehler: "You've hit your weekly limit - resets 5am (UTC)". Der Nutzer hatte vorab mitgeteilt, dass heute ohnehin der letzte Tag mit Fable-5-Verfuegbarkeit ist — ein Reset um 5 Uhr UTC aendert daran nichts Verlaessliches (Zugriffsfenster endet unabhaengig vom Quota-Reset). H-09, H-10, H-11 waren zu diesem Zeitpunkt bereits fertig, committet und gepusht. H-12 (4/5 Moduldateien fertig aus dem 1. Versuch, driver.py aus dem 2. Versuch VOR dem Abbruch fertiggestellt) und H-13 (alle 6 Moduldateien + CLI + Runner + README aus dem Versuch VOR dem Abbruch fertiggestellt, NUR Tests fehlten) waren beim Abbruch fast vollstaendig.
- **Frage:** Wie wird der Build zu Ende gefuehrt, wenn Fable 5 nicht mehr verfuegbar ist? Betrifft auch die geplante Phase C (Adversarial-Audit), die ebenfalls auf Fable 5 vorgesehen war.
- **Optionen & Entscheidung:**
  - **(a, GEWAEHLT) Fallback auf das Session-Standardmodell (Sonnet) fuer den Rest von Phase B (Test-Vervollstaendigung H-12/H-13) UND fuer Phase C (Adversarial-Audit).** Begruendung: kein inhaltlicher Blocker (Autonomie-Protokoll CLAUDE.md §4 — Modellverfuegbarkeit ist keine der drei zulaessigen Rueckfrage-Ausnahmen: fehlende Zugriffsrechte/Secrets, Geldeinsatz, destruktive Operation. Ein Modellwechsel ist eine reversible Orchestrator-Entscheidung). Die bereits von Fable 5 geschriebene, hochwertige Statistik-/Methodik-Substanz (RMT-Eigenzerlegung, SVI/RND/GPD-Fits, Cropper-Detektion etc.) bleibt vollstaendig erhalten — Sonnet uebernimmt NUR die verbleibenden mechanischen/verifizierenden Teile (Tests schreiben, CLI/Runner fuer H-12 nachziehen, spaeter Spec-Konformitaets-Audit).
  - (b, verworfen) Warten auf Fable-5-Reset (5 Uhr UTC) und pausieren: widerspricht der expliziten Nutzer-Vorgabe "die Umsetzung soll... am Stueck sein... alles soll autonom umgesetzt werden" und der Prognose "heute der letzte Tag" (ein Reset um 5 Uhr UTC bringt keine verlaessliche Fortsetzung).
  - (c, verworfen) Nur die fehlenden Teile ueberspringen (kein Test fuer H-12/H-13): widerspraeche der erhoehten Beweislast-Pflicht dieses Laufs (kein menschliches Review fuer 2 Wochen, siehe Netzwerk-Entwurf) — gerade Tests duerfen nicht fehlen.
- **Auswirkung auf Phase C (Adversarial-Audit):** urspruenglich als 5 parallele Fable-5-Agenten geplant (frische Instanz pro Modul, nicht der Builder selbst — Betriebsblindheit vermeiden). Diese Eigenschaft (frischer, unabhaengiger Pruef-Agent) ist wichtiger als das konkrete Modell — Sonnet-Audit-Agenten erfuellen dieselbe Funktion (zeilenweiser Spec-Abgleich durch eine Instanz, die nicht gebaut hat). Wird mit Sonnet fortgesetzt.
- **Begruendung:** Reversibelste Option (keine Code-Aenderung an bereits gebauter Substanz, nur Fortsetzung mit anderem Werkzeug); haelt die Torpfosten-/Verifikations-Disziplin vollstaendig aufrecht (jedes Modul durchlaeuft weiterhin Build+Test+Audit, nur das ausfuehrende Modell aendert sich).
- **Rueckbauweg:** entfaellt (reine Werkzeug-Entscheidung, kein inhaltlicher Eingriff; bei erneuter Fable-5-Verfuegbarkeit koennte eine spaetere Session die Sonnet-geschriebenen Tests/Audits durch Fable-5-Versionen ersetzen, ist aber nicht noetig — Qualitaetsstandard bleibt identisch).


---

### DEC-20-Korrektur (append-only, 2026-07-08) · Fable-5-Verfuegbarkeit laenger als angenommen
- **Korrektur:** Der Nutzer stellt klar, dass Fable 5 laenger verfuegbar ist als in DEC-20 angenommen (das Wochenlimit war eine temporaere Quota-Grenze, kein permanentes Zugriffsende). DEC-20 wird NICHT rueckwirkend geaendert (Registry-Disziplin — Entscheidungen sind append-only), sondern hier korrigiert fortgeschrieben.
- **Angepasste Entscheidung:** Ab sofort **Fable 5** fuer (a) **Phase C — Adversarial-Audit** aller 5 Welle-4-Module (Rueckkehr zum urspruenglichen Netzwerk-Entwurf) und (b) **jeden weiteren neuen Code** in diesem Lauf (Phase D Fix-Loop falls noetig).
- **Was NICHT rueckgaengig gemacht wird:** Die zwei zum Korrektur-Zeitpunkt bereits LAUFENDEN Sonnet-Agenten (H-12-Tests, H-13-Tests) werden NICHT abgebrochen — sie sind mit vollem Kontext mitten in der Fertigstellung reiner Test-Dateien (die zugrundeliegende Statistik-Substanz aller Module stammt bereits vollstaendig von Fable 5, DEC-20). Ein Abbruch waere verschwendete Arbeit ohne Qualitaetsgewinn. Sobald sie fertig sind, werden ihre Tests normal verifiziert und committet — kein Fable-5-Nachbau noetig.
- **Rueckbauweg:** entfaellt (reine Werkzeug-Fortfuehrung).
